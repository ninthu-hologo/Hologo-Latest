using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class appDataUpdater : MonoBehaviour
{
    // UDPATES FROM VERSION 121 to 200
    /* THIS CLASS DEALS WITH UPDATING AN OLD APP
     * 1. load all the important data stored by the old app
     * 2. user data, settings data , recorded lessons data
     * 3. after reading the data is converted to the new app version and saved
     * 4. and all the old data is deleted from storage.
     * 5. delete data include > library, categories , pics, assetbundles, user, config,
     * 6. lessons, audio files
     * 7. after that app updating is done and app will then continue to load new data.
     * 8. this class wont run again after that.
     * */

    // Start is called before the first frame update
    void Start()
    {
        
    }





}
